# 重叠切片训练实验状态

更新时间：2026-08-31 21:32（Asia/Shanghai）

## 已完成

- 已冻结 640 px 窗口、20% 名义重叠、50% 训练读取概率与筛选门槛，详见 `EXPERIMENT_PLAN.md`；
- 已实现切片窗口生成、候选小目标完整容纳、标注坐标重写和标签过滤；
- 已完成全部 52 项自动测试；
- 已完成 1 epoch / 1% 数据的单进程和 2-worker 冒烟训练；训练、验证、检查点保存和 Mosaic 缓冲均正常；
- 模型不变：2,772,349 参数，训练态约 19.2 GFLOPs。
- 正式训练第 1 轮已稳定完成：耗时 97.4 秒，mAP50-95 为 2.972%（预热期数据，不作为效果结论），已进入第 2 轮。

## 正在进行

- 正在进行 50 epoch、seed 0 的正式筛选训练；
- 正式输出目录：`runs/overlap_tiles_50e`；
- 正式日志：`logs/overlap_tiles_train.log`；
- 状态文件：`status.json`。

训练结束后流水线会自动完成总体指标与 COCO Small/Medium/Large AP 评估，生成指标对比图、训练曲线、切片训练批次示例及 `REPORT.md`。只有全部预设门槛通过，才进入多随机种子实验。
